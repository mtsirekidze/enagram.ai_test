// This service has been disabled for the manual testing version of the app.
// To re-enable AI features, restore the GoogleGenAI implementation.
export const generateGeorgianTestCases = async (difficulty: 'simple' | 'complex'): Promise<string[]> => {
  return [];
};